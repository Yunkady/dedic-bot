[project]
name = "dedic-bot"
version = "0.1.0"
description = "Telegram bot for dedicated VM sales"
requires-python = ">=3.11"
dependencies = [
    "aiogram==3.27.0",
    "python-dotenv>=1.0.1",
]

[tool.uv]
package = false
